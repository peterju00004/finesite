{
  "name": "@finesite/finesite",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "vite build && node scripts/generate-pages.js && node scripts/copy-static-projects.js",    "dev": "vite serve",
    "preview": "vite preview"
  }
}