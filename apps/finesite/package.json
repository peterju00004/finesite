{
  "name": "@finesite/finesite",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "vite build && node scripts/generate-pages.js",
    "dev": "vite serve",
    "preview": "vite preview"
  }
}